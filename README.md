# Weather App (Python)

A simple Python application that fetches real-time weather data using the **OpenWeatherMap API**.  
This project demonstrates how to use REST APIs, make HTTP requests, and parse JSON responses.

---

## 🔧 Features
- Fetches real-time temperature
- Shows humidity percentage
- Shows weather condition (ex: clear sky, rain, haze)
- Uses OpenWeatherMap API

---

## 📦 Requirements
Install the required library:

```bash
pip install requests
